import { Body, Controller, Get, Param, Patch } from "@nestjs/common"; import { Role } from "@prisma/client"; import { CurrentUser } from "../../common/decorators/current-user.decorator"; import { Roles } from "../../common/decorators/roles.decorator"; import { UpdatePolicyDto } from "./dto/update-policy.dto"; import { PoliciesService } from "./policies.service";
@Controller("policies") export class PoliciesController { constructor(private service: PoliciesService) {} @Get() findAll() { return this.service.findAll(); } @Roles(Role.ADMIN) @Patch(":id") update(@Param("id") id: string, @Body() dto: UpdatePolicyDto, @CurrentUser() user: { id: string }) { return this.service.update(id, dto, user.id); } }
