export function ModulePlaceholder({ title, fields }: { title: string; description: string; fields: string[] }) { return <section className="dataPanel modulePanel"><div className="dataHead"><div><h2>Danh sách {title.toLowerCase()}</h2><p>Scaffold sẵn sàng kết nối với backend API.</p></div><button className="primary">＋ Tạo mới</button></div><div className="tableTools"><label><span>⌕</span><input placeholder={`Tìm kiếm ${title.toLowerCase()}...`} /></label><button>⟳ Đặt lại</button></div><div className="placeholderTable"><div className="placeholderHeader">{fields.map(field => <span key={field}>{field}</span>)}</div><div className="placeholderEmpty"><b>Chưa có dữ liệu trực tiếp</b><p>Chạy backend và kết nối API để hiển thị dữ liệu {title.toLowerCase()}.</p></div></div></section>; }
